{
  "name": "ai-auto-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cursor-first AI browser test orchestration tools",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "validate-run": "node scripts/validate-run.mjs"
  }
}
